#!/usr/bin/env bash
xset s blank
xset s 600
xset dpms 1200 1800 2400
xsetroot -solid black
setxkbmap dvorak
[ -f ~/.xprofile ] && . ~/.xprofile
[ -e ~/.Xmodmap ] && xmodmap ~/.Xmodmap
redshift-altadena &
