# Transparency
opacity-rule = [
  "95:class_g = 'Electron' && name = 'Discord Overlay'"
];

# Fade effects (optional)
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;

# Backend
backend = "glx";
vsync = true;
use-damage = true;
